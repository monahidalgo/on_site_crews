name: on_site_crews
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  geolocator: ^8.2.1
  fluentui_system_icons: ^1.1.2
  audioplayers: ^2.0.0
  provider: ^6.0.0
  shared_preferences: ^2.0.15
  google_maps_flutter: ^2.2.0
  image_picker_web: ^2.0.0
 




  image_picker: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sounds/
